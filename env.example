# PHH VPN Client Configuration Example
# Copy this file to .env or set these as system environment variables

# Proxy Server IP Address
PROXY_IP=172.33.157.252

# Proxy Server Port
PROXY_PORT=8118

# Instructions:
# 1. Copy this file to .env: cp env.example .env
# 2. Edit .env and set your proxy IP and port
# 3. Or set as environment variables (recommended):
#    Linux/macOS: export PROXY_IP="192.168.1.100" && export PROXY_PORT="8080"
#    Windows: set PROXY_IP=192.168.1.100 && set PROXY_PORT=8080
# 4. Or manually enter in the application GUI
